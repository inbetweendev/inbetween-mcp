// Простые smoke tests
import { describe, it, expect } from "vitest";

describe("config", () => {
  it("works", () => {
    expect(1 + 1).toBe(2);
  });
});

// V1: добавим integration tests с mock backend
